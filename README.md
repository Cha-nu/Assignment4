# Assignment4
1.구현 의도 분석  
다대일(Many-to-One) 관계를 가진 데이터 구조의 관리를 목적으로 코드가 작성되었다.  
std::vector<std::string>을 사용하여 하나의 물약에 들어가는 재료의 개수를 유연하게 처리했다.  
사용자의 입력을 받아 실시간으로 레시피를 추가하고 조회하는 인터랙티브한 기능을 의도했다.  
  
2.기존 코드 수정을 최소화하는 개선 방안
데이터 은닉 및 인터페이스 제공
PotionRecipe의 멤버를 private으로 바꾸고 Getter를 제공한다.
AlchemyWorkshop 내부에서 std::cout을 제거하고, 데이터를 외부로 전달하거나 출력 전용 클래스를 만든다.
  
3. 원칙준수 여부분석  
SRP (단일 책임 원칙) 미흡 AlchemyWorkshop 클래스가 데이터 관리와 UI 출력이라는 두 가지 책임을 동시에 가지고 있다.  
OCP (개방-폐쇄 원칙) 미흡 파일 저장, GUI 등을 추가하려면 기존 클래스의 메서드를 직접 수정해야한다.  
DIP (의존 역전 원칙) 미흡 상위 모듈이 구체적인 PotionRecipe 클래스와 std::cout에 직접 의존하고 있어 결합도가 높다.  
